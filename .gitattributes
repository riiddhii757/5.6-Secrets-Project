# Set default behavior to automatically normalize line endings
* text=auto

# Treat certain file types as binary so Git doesn't change them
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.exe binary

# Force CRLF on Windows for scripts (optional)
*.bat text eol=crlf
*.cmd text eol=crlf

# Force LF for scripts used on Linux/Mac (optional)
*.sh text eol=lf
